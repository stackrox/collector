
# Golang version
GOVERSIONURI=https://dl.google.com/go/go1.11.5.linux-amd64.tar.gz

set -e
TOPCACHE=$HOME/.srbcache
DISTCACHE=$TOPCACHE/dist

test -d $DISTCACHE || mkdir -p $DISTCACHE

cd $DISTCACHE

if test ! -d golang ; then
	mkdir golang
	cd golang
	wget "$GOVERSIONURI"
fi

