---

- name: Get arch info
  shell: arch
  register: arch

- name: Get hostname
  shell: hostname
  register: hostname

- name: Destroy x86 VM
  delegate_to: localhost
  google.cloud.gcp_compute_instance:
    name: "{{ inventory_hostname }}"
    project: "{{ gcp_project }}"
    auth_kind: "{{ gcp_auth_kind }}"
    service_account_file: "{{ gcp_service_account_file }}"
    # zone is set by the GCP inventory automatically
    zone: "{{ zone }}"
    state: absent
  when: arch.stdout == "x86_64"

- name: Delete IBM Z VSI
  import_role: 
    name: destroy-s390x-vm
  when: arch.stdout == "s390x"
