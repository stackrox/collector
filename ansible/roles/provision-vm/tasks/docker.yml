---

- name: Reload service
  systemd:
    state: started
    daemon_reload: true
    name: docker

- name: Add user to docker group
  user:
    name: "{{ ansible_user }}"
    groups: docker
    append: yes

- name: Reset SSH connection for group propagation
  meta: reset_connection
