diff --git a/collector/container-plugin/ContainerPlugin.cpp b/collector/container-plugin/ContainerPlugin.cpp
--- a/collector/container-plugin/ContainerPlugin.cpp
+++ b/collector/container-plugin/ContainerPlugin.cpp
@@ -41,7 +41,7 @@
   if (state->reader->read_entry_field(state->cgroup_table, entry, state->plugin->cgroup_path, &value) != SS_PLUGIN_SUCCESS) {
     return 0;
   }
-  if (value.str != nullptr) {
+  if (value.str != nullptr && state->container_id.empty()) {
     state->container_id = ContainerIDFromCgroup(value.str);
   }
   return 1;
