FROM centos:8

RUN yum update -y
RUN yum -y groupinstall "Development Tools" && \
    yum -y install llvm-toolset elfutils-libelf-devel \
    ;

RUN mkdir -p /output
COPY build-kos /usr/bin/
COPY prepare-src /usr/bin

WORKDIR /scratch
ENTRYPOINT [ "/bin/bash", "-c" ]

