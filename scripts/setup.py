from setuptools import setup

setup(
    name="collector_tools",
    description="Useful functionality for collector builds and tooling",
    packages=["collector_tools"],
    install_requires=["google-cloud-compute"],
)
